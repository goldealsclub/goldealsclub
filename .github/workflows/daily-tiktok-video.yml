name: Daily TikTok video

on:
  schedule:
    # 06:00 UTC every day (07h Paris hiver / 08h Paris été)
    - cron: "0 6 * * *"
  workflow_dispatch:
    inputs:
      style:
        description: "Style à générer (vide = les trois)"
        required: false
        default: ""

jobs:
  cutout-qa:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    defaults:
      run:
        working-directory: remotion
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - name: Verify approved cutout references
        run: bun run qa:cutout
      - name: Upload cutout report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: cutout-qa-report
          path: |
            remotion/qa/cutout/current/*.png
            remotion/qa/cutout/diff/*.png
            remotion/qa/cutout/report.json
          retention-days: 14

  visual-qa:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        style: [adidas, zara, nike]
    defaults:
      run:
        working-directory: remotion
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: sudo apt-get update && sudo apt-get install -y chromium-browser ffmpeg
      - run: bun install --frozen-lockfile
      - name: Verify approved ${{ matrix.style }} frames
        env:
          PUPPETEER_EXECUTABLE_PATH: /usr/bin/chromium-browser
        run: bun run qa:${{ matrix.style }}

  generate:
    needs: [cutout-qa, visual-qa]
    runs-on: ubuntu-latest
    timeout-minutes: 35
    strategy:
      fail-fast: false
      matrix:
        style: [adidas, zara, nike]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Skip non-selected style
        if: ${{ github.event.inputs.style != '' && github.event.inputs.style != matrix.style }}
        run: echo "skip=true" >> $GITHUB_ENV

      - name: Setup Bun
        if: ${{ env.skip != 'true' }}
        uses: oven-sh/setup-bun@v2

      - name: Install ffmpeg + chromium
        if: ${{ env.skip != 'true' }}
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg chromium-browser

      - name: Install Remotion deps
        if: ${{ env.skip != 'true' }}
        working-directory: remotion
        run: bun install --frozen-lockfile

      - name: Generate video (${{ matrix.style }})
        if: ${{ env.skip != 'true' }}
        working-directory: remotion
        env:
          PUPPETEER_EXECUTABLE_PATH: /usr/bin/chromium-browser
        run: |
          sudo mkdir -p /mnt/documents
          sudo chmod 777 /mnt/documents
          node scripts/generate-variant.mjs top --style=${{ matrix.style }}

      - name: Publish to the site
        if: ${{ env.skip != 'true' }}
        env:
          VIDEO_PUBLISH_TOKEN: ${{ secrets.VIDEO_PUBLISH_TOKEN }}
        run: |
          VIDEO=$(ls -t /mnt/documents/goldeals-tiktok-*-${{ matrix.style }}-*.mp4 | head -n1)
          echo "Publishing $VIDEO"
          node remotion/scripts/publish-video.mjs "$VIDEO" \
            --style=${{ matrix.style }} \
            --category=top \
            --label="Sélection du jour"

      - name: Upload as workflow artifact (backup)
        if: ${{ env.skip != 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: tiktok-video-${{ matrix.style }}
          path: /mnt/documents/goldeals-tiktok-*-${{ matrix.style }}-*.mp4
          retention-days: 14
